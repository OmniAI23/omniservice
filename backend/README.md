# FastAPI Chatbot Application

This is a FastAPI application that allows users to create chatbots and provide them with information from various sources (files, URLs, YouTube videos) to build their knowledge base.

## Getting Started

Server should run automatically when starting a workspace. To run manually, run:
```sh
./devserver.sh
```